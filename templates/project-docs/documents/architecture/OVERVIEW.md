# Architecture Overview

## System Summary

<!--
  Describe what the system does in 2-3 sentences.
  Then list the high-level autonomous steps as a numbered list.
-->

## Component Architecture

<!--
  Draw an ASCII diagram showing the layers of your system.
  The proven pattern from Rudder has four layers:

1. Interface layer (CLI, API, UI) — user-facing, no business logic
  3. Service layer — external integrations behind interfaces (LLM, email, search, DB, etc.)
  4. State layer — persistence (files, DB, cache)

  Adapt the diagram below to your project.
-->
